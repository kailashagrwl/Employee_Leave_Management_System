# Database
MONGODB_URI=mongodb://username:password@host/dbname

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Environment
NODE_ENV=development
PORT=5000

# CORS - Allowed frontend origins (comma-separated, NO SPACES)
# For dev: localhost
# For prod: add your frontend URL (Vercel, Netlify, etc)
ALLOWED_ORIGINS=localhost,yourdomain.com,yourfrontend.vercel.app
